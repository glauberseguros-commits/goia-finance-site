import streamlit as st`nfrom utils.ui import aplicar_estilo_premium`nimport re`nimport sqlite3`nimport pytesseract`nfrom pdf2image import convert_from_bytes`nfrom pypdf import PdfReader`nfrom datetime import datetime`n`nDB_PATH = "bd/gofinance.db"`n`nfrom utils.auth import empresa_logada, exigir_login`n`nexigir_login()`nEMPRESA_ID_ATIVA = empresa_logada()`n`nst.set_page_config(`n    page_title="Importar Documento",`n    page_icon="📄",`n    layout="wide"`n)`n`naplicar_estilo_premium()`n`n`nst.markdown("""`n<style>`n[data-testid="stSidebarNav"] {`n    display: none;`n}`n</style>`n""", unsafe_allow_html=True)`n`n`ndef menu_goia():`n    st.sidebar.markdown("## GOIA")`n    st.sidebar.page_link("app.py", label="Dashboard", icon="🏠")`n    st.sidebar.page_link("pages/1_Importar_Documento.py", label="Importar Documento", icon="📄")`n    st.sidebar.page_link("pages/9_Clientes.py", label="Clientes", icon="👥")`n    st.sidebar.page_link("pages/10_Fornecedores.py", label="Fornecedores", icon="🏭")`n    st.sidebar.page_link("pages/2_Contas_a_Receber.py", label="Contas a Receber", icon="💰")`n    st.sidebar.page_link("pages/3_Contas_a_Pagar.py", label="Contas a Pagar", icon="💸")`n    st.sidebar.page_link("pages/7_Processos_Documentais.py", label="Processos Documentais", icon="🗂️")`n    st.sidebar.page_link("pages/8_Conciliacao_Bancaria.py", label="Conciliação Bancária", icon="🏦")`n`nmenu_goia()`n`n`n`nst.title("📄 Importar Documento Financeiro")`nst.caption("Triagem ERP: compra, venda, despesa, comprovante, extrato e cadastro.")`n`narquivos = st.file_uploader(`n    "Selecione um ou mais documentos PDF",`n    type=["pdf"],`n    accept_multiple_files=True`n)`n`nif arquivos:`n    st.info(f"{len(arquivos)} documento(s) selecionado(s). O sistema processará todos e classificará cada um no seu devido lugar.")`n`n`n# =========================`n# UTILITÁRIOS`n# =========================`n`ndef conectar():`n    return sqlite3.connect(DB_PATH)`n`ndef obter_cnpj_empresa_logada():`n    conn = conectar()`n    cur = conn.cursor()`n`n    try:`n        cur.execute("""`n            SELECT cnpj`n            FROM empresas`n            WHERE id = ?`n        """, (EMPRESA_ID_ATIVA,))`n        row = cur.fetchone()`n    except Exception:`n        row = None`n`n    conn.close()`n`n    if row and row[0]:`n        return normalizar_cnpj(row[0])`n`n    return ""`n`ndef normalizar_cnpj(cnpj):`n    numeros = re.sub(r"\D", "", cnpj or "")`n    if len(numeros) != 14:`n        return cnpj or ""`n    return f"{numeros[:2]}.{numeros[2:5]}.{numeros[5:8]}/{numeros[8:12]}-{numeros[12:]}"`n`nCNPJ_EMPRESA = obter_cnpj_empresa_logada()`n`ndef encontrar_cnpjs(texto):`n    from utils.validadores import validar_cnpj`n`n    encontrados = re.findall(`n        r"\d{2}\.?\d{3}\.?\d{3}/?\d{4}-?\d{2}",`n        texto or ""`n    )`n`n    validos = []`n`n    for cnpj in encontrados:`n        if validar_cnpj(cnpj):`n            validos.append(`n                normalizar_cnpj(cnpj)`n            )`n`n    return list(dict.fromkeys(validos))`n`ndef encontrar_datas(texto):`n    datas = re.findall(r"\b\d{2}/\d{2}/\d{4}\b", texto or "")`n    return list(dict.fromkeys(datas))`n`ndef converter_data(data_br):`n    if not data_br:`n        return None`n    try:`n        return datetime.strptime(data_br, "%d/%m/%Y").strftime("%Y-%m-%d")`n    except:`n        return None`n`ndef encontrar_valores(texto):`n    valores = re.findall(r"\d{1,3}(?:\.\d{3})*,\d{2}", texto or "")`n    return list(dict.fromkeys(valores))`n`ndef converter_valor(valor):`n    if not valor:`n        return 0.0`n    valor = str(valor).replace("R$", "").replace(" ", "")`n    valor = valor.replace(".", "").replace(",", ".")`n    try:`n        return float(valor)`n    except:`n        return 0.0`n`ndef maior_valor(texto):`n    valores = [converter_valor(v) for v in encontrar_valores(texto)]`n    valores = [v for v in valores if v > 0]`n    return max(valores) if valores else 0.0`n`ndef extrair_chave_acesso_nfe(texto):`n    t = texto or ""`n    grupos = re.findall(r"(?:\d{4}\s*){11}", t)`n`n    for grupo in grupos:`n        chave = re.sub(r"\D", "", grupo)`n        if len(chave) == 44:`n            return chave`n`n    return ""`n`n`ndef formatar_chave_acesso(chave):`n    chave = re.sub(r"\D", "", chave or "")`n    if len(chave) != 44:`n        return chave`n    return " ".join(chave[i:i+4] for i in range(0, 44, 4))`n`n`ndef extrair_numero_serie_nfe(texto, chave_acesso=""):`n    chave = re.sub(r"\D", "", chave_acesso or "")`n    numero = ""`n    serie = ""`n`n    if len(chave) == 44:`n        serie = chave[22:25].lstrip("0") or "0"`n        numero = chave[25:34].lstrip("0") or "0"`n`n    return numero, serie`n`ndef extrair_texto_pypdf(arquivo):`n    arquivo.seek(0)`n    reader = PdfReader(arquivo)`n    texto = ""`n    for page in reader.pages:`n        texto += page.extract_text() or ""`n    return texto.strip()`n`ndef extrair_texto_ocr(arquivo):`n    arquivo.seek(0)`n    imagens = convert_from_bytes(arquivo.read(), dpi=300)`n    texto = ""`n    for imagem in imagens:`n        texto += pytesseract.image_to_string(imagem, lang="por+eng")`n    return texto.strip()`n`ndef extrair_texto_pdf(arquivo):`n    texto = extrair_texto_pypdf(arquivo)`n    if len(texto) < 50:`n        st.warning("PDF com pouco texto detectado. Aplicando OCR...")`n        texto = extrair_texto_ocr(arquivo)`n    return texto`n`n# =========================`n# CLASSIFICAÇÃO`n# =========================`n`ndef classificar_tipo_documento(texto):`n    t = (texto or "").upper()`n`n    # Regra central:`n    # O tipo do documento principal tem prioridade sobre referências internas.`n    # Uma NF-e pode citar nota de empenho, processo, licitação ou contrato nas informações complementares.`n    if (`n        "DANFE" in t`n        or "NF-E" in t`n        or "NFE" in t`n        or "NOTA FISCAL" in t`n        or "NOTA FISCAL ELETRONICA" in t`n        or "NOTA FISCAL ELETRÔNICA" in t`n        or "CHAVE DE ACESSO" in t`n        or "DOCUMENTO AUXILIAR DA NOTA FISCAL" in t`n    ):`n        return "Nota Fiscal"`n`n    if "FICHA DE COMPENSAÇÃO" in t or "BOLETO" in t or "PAGAR PREFERENCIALMENTE" in t or "PAGÁVEL PREFERENCIALMENTE" in t:`n        return "Boleto / Despesa"`n`n    if "COMPROVANTE" in t and ("PIX" in t or "TRANSFERÊNCIA" in t or "TRANSFERENCIA" in t or "PAGAMENTO" in t or "RECEBIMENTO" in t):`n        if "RECEBEMOS" in t or "RECEBIMENTO" in t:`n            return "Comprovante de Recebimento"`n        return "Comprovante de Pagamento"`n`n    if "EXTRATO" in t and ("SALDO" in t or "LANÇAMENTO" in t or "LANCAMENTO" in t):`n        return "Extrato Bancário"`n`n    if "CADASTRO NACIONAL DA PESSOA JURÍDICA" in t:`n        return "Cartão CNPJ"`n`n    if (`n        "NOTA DE EMPENHO" in t`n        or " EMPENHO " in t`n        or "NE00" in t`n    ):`n        return "Nota de Empenho"`n`n    return "A classificar"`n`n`ndef sugerir_direcao_nf(texto):`n    t = (texto or "").upper()`n`n    if "COMANDO DA MARINHA" in t:`n        return "Nota Fiscal de Venda"`n`n    if "DESTINATÁRIO" in t and CNPJ_EMPRESA in texto:`n        return "Nota Fiscal de Compra"`n`n    return "Nota Fiscal de Compra"`n`ndef identificar_nome_por_cnpj(texto, cnpj):`n    if not cnpj:`n        return ""`n`n    linhas = [l.strip() for l in (texto or "").splitlines() if l.strip()]`n    for i, linha in enumerate(linhas):`n        if cnpj in linha or re.sub(r"\D", "", cnpj) in re.sub(r"\D", "", linha):`n            candidatos = []`n            if i > 0:`n                candidatos.append(linhas[i - 1])`n            if i > 1:`n                candidatos.append(linhas[i - 2])`n            if i + 1 < len(linhas):`n                candidatos.append(linhas[i + 1])`n`n            for c in candidatos:`n                c_up = c.upper()`n                if len(c) >= 5 and not re.search(r"\d{2}/\d{2}/\d{4}", c) and "CNPJ" not in c_up:`n                    return c[:120]`n    return ""`n`ndef extrair_valor_total_nfe(texto):`n    t = texto or ""`n    linhas = [l.strip() for l in t.splitlines() if l.strip()]`n    texto_up = t.upper()`n`n    # 1) Caso OCR da DANFE traga o rótulo quebrado: V. TOTAL DE PRODUTOS / V. TOTAL DA NOTA`n    rotulos_prioritarios = [`n        "V. TOTAL DA NOTA",`n        "VALOR TOTAL DA NOTA",`n        "V TOTAL DA NOTA",`n        "VALOR TOTAL DOS PRODUTOS",`n        "V. TOTAL DE PRODUTOS",`n        "V TOTAL DE PRODUTOS",`n        "TOTAL DE PRODUTOS"`n    ]`n`n    for i, linha in enumerate(linhas):`n        linha_up = linha.upper()`n`n        if any(rotulo in linha_up for rotulo in rotulos_prioritarios):`n            janela = " ".join(linhas[i:i+8])`n            valores = encontrar_valores(janela)`n            valores_validos = []`n`n            for v in valores:`n                valor = converter_valor(v)`n                # Evita zeros de impostos e evita valores absurdos de CNPJ/chave.`n                if 0 < valor < 100000000:`n                    valores_validos.append(valor)`n`n            if valores_validos:`n                return max(valores_validos)`n`n    # 2) Caso apareça "V. TOTAL DA NOTA 2.598,65" na mesma linha`n    padroes = [`n        r"V\.\s*TOTAL\s*DA\s*NOTA\s*(?:R\$)?\s*([\d\.\,]+)",`n        r"VALOR\s*TOTAL\s*DA\s*NOTA\s*(?:R\$)?\s*([\d\.\,]+)",`n        r"V\.\s*TOTAL\s*DE\s*PRODUTOS\s*(?:R\$)?\s*([\d\.\,]+)",`n        r"VALOR\s*TOTAL\s*DOS\s*PRODUTOS\s*(?:R\$)?\s*([\d\.\,]+)",`n        r"VALOR\s*A\s*PAGAR\s*(?:R\$)?\s*([\d\.\,]+)",`n        r"TOTAL\s*GERAL\s*(?:R\$)?\s*([\d\.\,]+)"`n    ]`n`n    for padrao in padroes:`n        m = re.search(padrao, t, flags=re.IGNORECASE)`n        if m:`n            valor = converter_valor(m.group(1))`n            if valor > 0:`n                return valor`n`n    # 3) Fallback específico para itens da NF-e.`n    # Procura linhas de produto/serviço e captura valores monetários nelas.`n    palavras_item = [`n        "MAQUINA",`n        "MÁQUINA",`n        "PRODUTO",`n        "SERVICO",`n        "SERVIÇO",`n        "MATRIZ",`n        "CHAVE",`n        "UN[",`n        "UN "`n    ]`n`n    candidatos_item = []`n`n    for linha in linhas:`n        linha_up = linha.upper()`n`n        if any(p in linha_up for p in palavras_item):`n            valores_linha = encontrar_valores(linha)`n`n            for v in valores_linha:`n                valor = converter_valor(v)`n`n                if valor <= 0:`n                    continue`n`n                # Evita CNPJ OCRizado, chave de acesso, protocolo e valores absurdos.`n                if valor >= 100000:`n                    continue`n`n                candidatos_item.append(valor)`n`n    if candidatos_item:`n        return max(candidatos_item)`n`n    # Para NF-e, se não encontrou rótulo nem item, não inventa valor.`n    return 0.0`n`ndef extrair_emitente_destinatario_nfe(texto):`n    linhas = [l.strip() for l in (texto or "").splitlines() if l.strip()]`n    t = texto or ""`n    t_up = t.upper()`n    cnpjs = encontrar_cnpjs(texto)`n`n    emitente_nome = ""`n    emitente_cnpj = ""`n    destinatario_nome = ""`n    destinatario_cnpj = ""`n`n    # Emitente conhecido: própria empresa GODS.`n    if (`n        "GODS PRODUTOS" in t_up`n        or "GO LICITAÇÕES" in t_up`n        or "GO LICITACOES" in t_up`n        or CNPJ_EMPRESA in cnpjs`n    ):`n        emitente_nome = "GODS PRODUTOS SERVICOS & EVENTOS LTDA"`n        emitente_cnpj = CNPJ_EMPRESA`n`n    # Emitente conhecido: fornecedor Ponto Certo.`n    if "PONTO CERTO" in t_up:`n        emitente_nome = "PONTO CERTO COMERCIO DE FERRAGENS LTDA"`n        emitente_cnpj = "11.877.694/0001-66"`n`n    # Destinatário conhecido: Marinha.`n    if "COMANDO DA MARINHA" in t_up:`n        destinatario_nome = "COMANDO DA MARINHA"`n        for c in cnpjs:`n            if c != CNPJ_EMPRESA:`n                destinatario_cnpj = c`n                break`n`n    # Bloco destinatário/remetente.`n    for i, linha in enumerate(linhas):`n        linha_up = linha.upper()`n`n        if "DESTINATÁRIO" in linha_up or "DESTINATARIO" in linha_up or "REMETENTE" in linha_up:`n            trecho = " ".join(linhas[i:i+12])`n            trecho_up = trecho.upper()`n            cnpjs_trecho = encontrar_cnpjs(trecho)`n`n            if "COMANDO DA MARINHA" in trecho_up:`n                destinatario_nome = "COMANDO DA MARINHA"`n`n            for c in cnpjs_trecho:`n                if c != CNPJ_EMPRESA:`n                    destinatario_cnpj = c`n                    nome = identificar_nome_por_cnpj(trecho, c)`n                    if nome and "CHAVE DE ACESSO" not in nome.upper():`n                        destinatario_nome = nome`n                    break`n`n            if CNPJ_EMPRESA in cnpjs_trecho and not destinatario_cnpj:`n                destinatario_cnpj = CNPJ_EMPRESA`n                destinatario_nome = "GODS PRODUTOS SERVICOS & EVENTOS LTDA"`n`n    # Se a empresa própria não for emitente, mas aparece como destinatária, corrigir compra.`n    if emitente_cnpj == CNPJ_EMPRESA and destinatario_cnpj == CNPJ_EMPRESA:`n        destinatario_cnpj = ""`n        destinatario_nome = ""`n`n    # Fallback: se ainda não tem destinatário e há CNPJ diferente da empresa.`n    if not destinatario_cnpj:`n        for c in cnpjs:`n            if c != CNPJ_EMPRESA:`n                destinatario_cnpj = c`n                nome = identificar_nome_por_cnpj(texto, c)`n                if nome and "CHAVE DE ACESSO" not in nome.upper():`n                    destinatario_nome = nome`n                break`n`n    # Se não achou emitente e há CNPJ diferente da empresa, assumir fornecedor externo.`n    if not emitente_cnpj:`n        for c in cnpjs:`n            if c != CNPJ_EMPRESA:`n                emitente_cnpj = c`n                nome = identificar_nome_por_cnpj(texto, c)`n                emitente_nome = nome or ""`n                break`n`n    return {`n        "emitente_nome": emitente_nome,`n        "emitente_cnpj": emitente_cnpj,`n        "destinatario_nome": destinatario_nome,`n        "destinatario_cnpj": destinatario_cnpj`n    }`n`n`n`ndef extrair_descricao_produto_nfe(texto):`n    linhas = [l.strip() for l in (texto or "").splitlines() if l.strip()]`n`n    for linha in linhas:`n        linha_up = linha.upper()`n`n        if "MAQUINA" in linha_up or "MÁQUINA" in linha_up:`n            partes = linha.split("|")`n`n            for parte in partes:`n                p = parte.strip()`n`n                if "MAQUINA" in p.upper() or "MÁQUINA" in p.upper():`n                    p = p.replace("[", " ").replace("]", " ")`n                    p = " ".join(p.split())`n                    return p[:180]`n`n            return " ".join(linha.split())[:180]`n`n    return ""`n`ndef analisar_documento(texto):`n    cnpjs = encontrar_cnpjs(texto)`n    datas = encontrar_datas(texto)`n`n    tipo_detectado = classificar_tipo_documento(texto)`n`n    if tipo_detectado == "Nota de Empenho":`n`n        nome_orgao = "Órgão Público"`n`n        texto_up = (texto or "").upper()`n`n        if "ESTADO-MAIOR DA ARMADA" in texto_up:`n            nome_orgao = "ESTADO-MAIOR DA ARMADA"`n`n        elif "COMANDO DA MARINHA" in texto_up:`n            nome_orgao = "COMANDO DA MARINHA"`n`n        return {`n            "tipo_detectado": "Nota de Empenho",`n            "direcao_sugerida": "Nota de Empenho",`n            "cnpjs": [],`n            "parte_cnpj": "",`n            "parte_nome": nome_orgao,`n            "valor": maior_valor(texto),`n            "data_emissao": None,`n            "data_vencimento": None,`n            "chave_acesso_nfe": "",`n            "numero_nfe": "",`n            "serie_nfe": ""`n        }`n`n    valor = extrair_valor_total_nfe(texto) if tipo_detectado == "Nota Fiscal" else maior_valor(texto)`n`n    chave_acesso_nfe = extrair_chave_acesso_nfe(texto) if tipo_detectado == "Nota Fiscal" else ""`n    numero_nfe, serie_nfe = extrair_numero_serie_nfe(texto, chave_acesso_nfe) if tipo_detectado == "Nota Fiscal" else ("", "")`n`n    partes_nfe = extrair_emitente_destinatario_nfe(texto) if tipo_detectado == "Nota Fiscal" else {}`n`n    emitente_cnpj = partes_nfe.get("emitente_cnpj", "")`n    emitente_nome = partes_nfe.get("emitente_nome", "")`n    destinatario_cnpj = partes_nfe.get("destinatario_cnpj", "")`n    destinatario_nome = partes_nfe.get("destinatario_nome", "")`n`n    if tipo_detectado == "Nota Fiscal":`n        if emitente_cnpj == CNPJ_EMPRESA:`n            direcao_sugerida = "Nota Fiscal de Venda"`n            parte_cnpj = destinatario_cnpj`n            parte_nome = destinatario_nome`n        elif destinatario_cnpj == CNPJ_EMPRESA:`n            direcao_sugerida = "Nota Fiscal de Compra"`n            parte_cnpj = emitente_cnpj`n            parte_nome = emitente_nome`n        else:`n            direcao_sugerida = "Nota Fiscal de Compra"`n            parte_cnpj = emitente_cnpj`n            parte_nome = emitente_nome`n    else:`n        direcao_sugerida = tipo_detectado`n        cnpjs_sem_empresa = [c for c in cnpjs if c != CNPJ_EMPRESA]`n        parte_cnpj = cnpjs_sem_empresa[0] if cnpjs_sem_empresa else ""`n        parte_nome = identificar_nome_por_cnpj(texto, parte_cnpj)`n`n    data_emissao = converter_data(datas[0]) if datas else None`n    data_vencimento = converter_data(datas[-1]) if datas else None`n`n    return {`n        "tipo_detectado": tipo_detectado,`n        "direcao_sugerida": direcao_sugerida,`n        "cnpjs": cnpjs,`n        "parte_cnpj": parte_cnpj,`n        "parte_nome": parte_nome,`n        "valor": valor,`n        "data_emissao": data_emissao,`n        "data_vencimento": data_vencimento,`n        "chave_acesso_nfe": chave_acesso_nfe,`n        "numero_nfe": numero_nfe,`n        "serie_nfe": serie_nfe`n    }`n`n# =========================`n# BANCO ERP`n# =========================`n`ndef obter_ou_criar_fornecedor(cursor, cnpj, nome):`n    cursor.execute("""`n        SELECT id`n        FROM fornecedores`n        WHERE cnpj = ?`n          AND empresa_id = ?`n    """, (cnpj, EMPRESA_ID_ATIVA))`n`n    resultado = cursor.fetchone()`n`n    if resultado:`n        return resultado[0]`n`n    cursor.execute("""`n        INSERT INTO fornecedores (`n            cnpj,`n            nome,`n            categoria_padrao,`n            tipo_padrao,`n            empresa_id`n        )`n        VALUES (?, ?, ?, ?, ?)`n    """, (`n        cnpj,`n        nome,`n        "A classificar",`n        "Pagar",`n        EMPRESA_ID_ATIVA`n    ))`n`n    return cursor.lastrowid`n`n`ndef obter_ou_criar_cliente(cursor, cnpj_cpf, nome):`n    if cnpj_cpf:`n        cursor.execute("""`n            SELECT id`n            FROM clientes`n            WHERE cnpj_cpf = ?`n              AND empresa_id = ?`n        """, (cnpj_cpf, EMPRESA_ID_ATIVA))`n    else:`n        cursor.execute("""`n            SELECT id`n            FROM clientes`n            WHERE nome = ?`n              AND empresa_id = ?`n        """, (nome, EMPRESA_ID_ATIVA))`n`n    resultado = cursor.fetchone()`n`n    if resultado:`n        return resultado[0]`n`n    cursor.execute("""`n        INSERT INTO clientes (`n            cnpj_cpf,`n            nome,`n            empresa_id`n        )`n        VALUES (?, ?, ?)`n    """, (`n        cnpj_cpf,`n        nome,`n        EMPRESA_ID_ATIVA`n    ))`n`n    return cursor.lastrowid`n`n`ndef obter_ou_criar_produto(cursor, descricao, custo=0, preco_venda=0):`n    cursor.execute("""`n        SELECT id`n        FROM produtos`n        WHERE descricao = ?`n          AND empresa_id = ?`n    """, (descricao, EMPRESA_ID_ATIVA))`n`n    resultado = cursor.fetchone()`n`n    if resultado:`n        produto_id = resultado[0]`n`n        cursor.execute("""`n            UPDATE produtos`n            SET`n                custo_medio = CASE WHEN ? > 0 THEN ? ELSE custo_medio END,`n                preco_venda = CASE WHEN ? > 0 THEN ? ELSE preco_venda END`n            WHERE id = ?`n              AND empresa_id = ?`n        """, (`n            custo,`n            custo,`n            preco_venda,`n            preco_venda,`n            produto_id,`n            EMPRESA_ID_ATIVA`n        ))`n`n        return produto_id`n`n    cursor.execute("""`n        INSERT INTO produtos (`n            descricao,`n            categoria,`n            custo_medio,`n            preco_venda,`n            empresa_id`n        )`n        VALUES (?, ?, ?, ?, ?)`n    """, (`n        descricao,`n        "A classificar",`n        custo,`n        preco_venda,`n        EMPRESA_ID_ATIVA`n    ))`n`n    return cursor.lastrowid`n`n`ndef criar_processo_documental_automatico(`n    cursor,`n    documento_id,`n    tipo_documento,`n    direcao,`n    parte_cnpj,`n    parte_nome,`n    valor,`n    numero_nfe="",`n    serie_nfe=""`n):`n    cursor.execute("""`n        SELECT id`n        FROM processos_documentais`n        WHERE empresa_id = ?`n          AND id IN (`n              SELECT processo_id`n              FROM processo_documentos`n              WHERE documento_id = ?`n                AND empresa_id = ?`n          )`n    """, (`n        EMPRESA_ID_ATIVA,`n        documento_id,`n        EMPRESA_ID_ATIVA`n    ))`n`n    existente = cursor.fetchone()`n`n    if existente:`n        return existente[0]`n`n    titulo = "Documento ID " + str(documento_id)`n`n    if numero_nfe:`n        titulo = f"NF-e {numero_nfe} Série {serie_nfe}"`n`n    if direcao == "Nota Fiscal de Compra":`n        tipo_operacao = "Compra"`n        papel_empresa = "Destinatário"`n        natureza = "Produto"`n        proxima_acao = "Anexar comprovante de pagamento"`n`n        pendencias = [`n            ("Comprovante de pagamento não localizado", "Comprovante de pagamento"),`n            ("Extrato bancário não conciliado", "Extrato bancário")`n        ]`n`n    elif direcao == "Nota Fiscal de Venda":`n        tipo_operacao = "Venda"`n        papel_empresa = "Emitente"`n        natureza = "Produto/Serviço"`n        proxima_acao = "Confirmar entrega ou execução e prazo de recebimento"`n`n        pendencias = [`n            ("Entrega ou execução não confirmada", "Comprovante de entrega/execução"),`n            ("Prazo de recebimento não informado", "Prazo de pagamento"),`n            ("Recebimento bancário não conciliado", "Extrato bancário")`n        ]`n`n    elif direcao == "Boleto / Despesa":`n        tipo_operacao = "Despesa"`n        papel_empresa = "Pagador"`n        natureza = "Despesa"`n        proxima_acao = "Anexar comprovante de pagamento"`n`n        pendencias = [`n            ("Comprovante de pagamento não localizado", "Comprovante de pagamento"),`n            ("Extrato bancário não conciliado", "Extrato bancário")`n        ]`n`n    else:`n        tipo_operacao = "A classificar"`n        papel_empresa = "A classificar"`n        natureza = "A classificar"`n        proxima_acao = "Classificar documento e definir evidências obrigatórias"`n`n        pendencias = [`n            ("Classificação documental pendente", "Classificação manual")`n        ]`n`n    cursor.execute("""`n        INSERT INTO processos_documentais (`n            titulo,`n            tipo_operacao,`n            papel_empresa,`n            natureza,`n            contraparte_nome,`n            contraparte_cnpj,`n            valor_total,`n            status,`n            proxima_acao,`n            empresa_id`n        )`n        VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`n    """, (`n        titulo,`n        tipo_operacao,`n        papel_empresa,`n        natureza,`n        parte_nome,`n        parte_cnpj,`n        valor,`n        "Aberto",`n        proxima_acao,`n        EMPRESA_ID_ATIVA`n    ))`n`n    processo_id = cursor.lastrowid`n`n    cursor.execute("""`n        INSERT INTO processo_documentos (`n            processo_id,`n            documento_id,`n            tipo_documento,`n            obrigatorio,`n            status,`n            empresa_id`n        )`n        VALUES (?, ?, ?, ?, ?, ?)`n    """, (`n        processo_id,`n        documento_id,`n        tipo_documento,`n        "Sim",`n        "Anexado",`n        EMPRESA_ID_ATIVA`n    ))`n`n    for descricao, tipo_evidencia in pendencias:`n        cursor.execute("""`n            INSERT INTO processo_pendencias (`n                processo_id,`n                descricao,`n                tipo_evidencia,`n                status,`n                empresa_id`n            )`n            VALUES (?, ?, ?, ?, ?)`n        """, (`n            processo_id,`n            descricao,`n            tipo_evidencia,`n            "Pendente",`n            EMPRESA_ID_ATIVA`n        ))`n`n    return processo_id`n`n`n`ndef salvar_documento_erp(nome_arquivo, tipo_documento, direcao, parte_cnpj, parte_nome, valor, data_emissao, data_vencimento, texto, chave_acesso_nfe="", numero_nfe="", serie_nfe=""):`n    conn = conectar()`n    cursor = conn.cursor()`n`n    if direcao == "Nota Fiscal de Compra":`n        cnpj_emitente = parte_cnpj`n        nome_emitente = parte_nome`n        cnpj_destinatario = CNPJ_EMPRESA`n        nome_destinatario = "GODS PRODUTOS, SERVIÇOS & EVENTOS LTDA"`n    elif direcao == "Nota Fiscal de Venda":`n        cnpj_emitente = CNPJ_EMPRESA`n        nome_emitente = "GODS PRODUTOS, SERVIÇOS & EVENTOS LTDA"`n        cnpj_destinatario = parte_cnpj`n        nome_destinatario = parte_nome`n    else:`n        cnpj_emitente = parte_cnpj`n        nome_emitente = parte_nome`n        cnpj_destinatario = ""`n        nome_destinatario = ""`n`n    if chave_acesso_nfe:`n        cursor.execute("""`n            SELECT id`n            FROM documentos`n            WHERE empresa_id = ?`n              AND chave_acesso_nfe = ?`n        """, (`n            EMPRESA_ID_ATIVA,`n            chave_acesso_nfe`n        ))`n    elif numero_nfe and serie_nfe:`n        cursor.execute("""`n            SELECT id`n            FROM documentos`n            WHERE empresa_id = ?`n              AND numero_nfe = ?`n              AND serie_nfe = ?`n              AND IFNULL(cnpj_emitente, '') = ?`n              AND IFNULL(cnpj_destinatario, '') = ?`n        """, (`n            EMPRESA_ID_ATIVA,`n            numero_nfe,`n            serie_nfe,`n            cnpj_emitente or "",`n            cnpj_destinatario or ""`n        ))`n    else:`n        cursor.execute("""`n            SELECT id`n            FROM documentos`n            WHERE empresa_id = ?`n              AND nome_arquivo = ?`n              AND valor = ?`n              AND IFNULL(cnpj_emitente, '') = ?`n              AND IFNULL(cnpj_destinatario, '') = ?`n              AND IFNULL(data_emissao, '') = ?`n        """, (`n            EMPRESA_ID_ATIVA,`n            nome_arquivo,`n            abs(valor),`n            cnpj_emitente or "",`n            cnpj_destinatario or "",`n            data_emissao or ""`n        ))`n`n    documento_existente = cursor.fetchone()`n`n    if documento_existente:`n        conn.close()`n        return {`n            "documento_id": documento_existente[0],`n            "ja_existia": True`n        }`n`n    cursor.execute("""`n        INSERT INTO documentos (`n            nome_arquivo,`n            tipo_documento,`n            direcao,`n            cnpj_emitente,`n            nome_emitente,`n            cnpj_destinatario,`n            nome_destinatario,`n            valor,`n            data_emissao,`n            data_vencimento,`n            status_processamento,`n            texto_extraido,`n            empresa_id,`n            chave_acesso_nfe,`n            numero_nfe,`n            serie_nfe`n        )`n        VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`n    """, (`n        nome_arquivo,`n        tipo_documento,`n        direcao,`n        cnpj_emitente,`n        nome_emitente,`n        cnpj_destinatario,`n        nome_destinatario,`n        abs(valor),`n        data_emissao,`n        data_vencimento,`n        "Processado",`n        texto,`n        EMPRESA_ID_ATIVA,`n        chave_acesso_nfe,`n        numero_nfe,`n        serie_nfe`n    ))`n`n    documento_id = cursor.lastrowid`n`n    descricao_operacao = "Documento importado"`n`n    if tipo_documento == "Nota Fiscal":`n        descricao_extraida = extrair_descricao_produto_nfe(texto)`n        if descricao_extraida:`n            descricao_operacao = descricao_extraida`n`n    if direcao in ["Nota Fiscal de Compra", "Boleto / Despesa"]:`n        fornecedor_id = obter_ou_criar_fornecedor(cursor, parte_cnpj, parte_nome)`n        produto_id = obter_ou_criar_produto(cursor, descricao_operacao, custo=abs(valor), preco_venda=0)`n`n        cursor.execute("""`n            INSERT INTO compras (`n                fornecedor_id,`n                documento_id,`n                descricao,`n                valor_total,`n                data_compra,`n                status,`n                empresa_id`n            )`n            VALUES (?, ?, ?, ?, ?, ?, ?)`n        """, (`n            fornecedor_id,`n            documento_id,`n            descricao_operacao,`n            abs(valor),`n            data_emissao,`n            "Aberta",`n            EMPRESA_ID_ATIVA`n        ))`n`n        compra_id = cursor.lastrowid`n`n        cursor.execute("""`n            INSERT INTO compras_itens (`n                compra_id,`n                produto_id,`n                descricao,`n                quantidade,`n                valor_unitario,`n                valor_total,`n                empresa_id`n            )`n            VALUES (?, ?, ?, ?, ?, ?, ?)`n        """, (`n            compra_id,`n            produto_id,`n            descricao_operacao,`n            1,`n            abs(valor),`n            abs(valor),`n            EMPRESA_ID_ATIVA`n        ))`n`n        cursor.execute("""`n            INSERT INTO contas_pagar (`n                fornecedor_id,`n                documento_id,`n                descricao,`n                categoria,`n                valor,`n                data_emissao,`n                data_vencimento,`n                status,`n                empresa_id`n            )`n            VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)`n        """, (`n            fornecedor_id,`n            documento_id,`n            descricao_operacao,`n            "Compras",`n            abs(valor),`n            data_emissao,`n            data_vencimento,`n            "Pendente",`n            EMPRESA_ID_ATIVA`n        ))`n`n    elif direcao == "Nota de Empenho":`n        if parte_nome:`n            obter_ou_criar_cliente(cursor, parte_cnpj, parte_nome)`n`n    elif direcao == "Nota Fiscal de Venda":`n        cliente_id = obter_ou_criar_cliente(cursor, parte_cnpj, parte_nome)`n        produto_id = obter_ou_criar_produto(cursor, descricao_operacao, custo=0, preco_venda=abs(valor))`n`n        cursor.execute("""`n            INSERT INTO vendas (`n                cliente_id,`n                documento_id,`n                descricao,`n                valor_total,`n                data_venda,`n                status,`n                empresa_id`n            )`n            VALUES (?, ?, ?, ?, ?, ?, ?)`n        """, (`n            cliente_id,`n            documento_id,`n            descricao_operacao,`n            abs(valor),`n            data_emissao,`n            "Aberta",`n            EMPRESA_ID_ATIVA`n        ))`n`n        venda_id = cursor.lastrowid`n`n        cursor.execute("""`n            INSERT INTO vendas_itens (`n                venda_id,`n                produto_id,`n                descricao,`n                quantidade,`n                valor_unitario,`n                valor_total,`n                empresa_id`n            )`n            VALUES (?, ?, ?, ?, ?, ?, ?)`n        """, (`n            venda_id,`n            produto_id,`n            descricao_operacao,`n            1,`n            abs(valor),`n            abs(valor),`n            EMPRESA_ID_ATIVA`n        ))`n`n        cursor.execute("""`n            INSERT INTO contas_receber (`n                cliente_id,`n                documento_id,`n                descricao,`n                categoria,`n                valor,`n                data_emissao,`n                data_vencimento,`n                status,`n                empresa_id`n            )`n            VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)`n        """, (`n            cliente_id,`n            documento_id,`n            descricao_operacao,`n            "Vendas",`n            abs(valor),`n            data_emissao,`n            data_vencimento,`n            "Pendente",`n            EMPRESA_ID_ATIVA`n        ))`n`n    processo_id = criar_processo_documental_automatico(`n        cursor,`n        documento_id,`n        tipo_documento,`n        direcao,`n        parte_cnpj,`n        parte_nome,`n        valor,`n        numero_nfe,`n        serie_nfe`n    )`n`n    conn.commit()`n    conn.close()`n`n    return {`n        "documento_id": documento_id,`n        "ja_existia": False,`n        "processo_id": processo_id`n    }`n`n`n# =========================`n# INTERFACE`n# =========================`n`nif arquivos:`n    processar_todos = st.button("Processar todos os documentos no ERP")`n`n    if processar_todos:`n        st.subheader("Resultado do processamento")`n`n        for arquivo in arquivos:`n            try:`n                texto = extrair_texto_pdf(arquivo)`n                analise = analisar_documento(texto)

                with st.expander(f"🔎 Diagnóstico técnico — {arquivo.name}", expanded=True):
                    st.json(analise)`n`n                resultado = salvar_documento_erp(`n                    arquivo.name,`n                    analise["tipo_detectado"],`n                    analise["direcao_sugerida"],`n                    analise["parte_cnpj"],`n                    analise["parte_nome"],`n                    analise["valor"],`n                    analise["data_emissao"] or None,`n                    analise["data_vencimento"] or None,`n                    texto,`n                    analise["chave_acesso_nfe"],`n                    analise["numero_nfe"],`n                    analise["serie_nfe"]`n                )`n`n                with st.container(border=True):`n                    st.markdown(f"### 📄 {arquivo.name}")`n                    st.write(f"**Tipo:** {analise['tipo_detectado']}")`n                    st.write(f"**Classificação:** {analise['direcao_sugerida']}")`n                    st.write(f"**Contraparte:** {analise['parte_nome'] or 'Não identificada'}")`n                    st.write(f"**CNPJ/CPF:** {analise['parte_cnpj'] or 'Não identificado'}")`n                    st.write(f"**Valor:** R$ {analise['valor']:,.2f}".replace(",", "X").replace(".", ",").replace("X", "."))`n`n                    if resultado.get("ja_existia"):`n                        st.warning(f"Já existia. Documento ID: {resultado['documento_id']}.")`n                    elif analise["direcao_sugerida"] == "Nota Fiscal de Venda":`n                        st.success(f"Cliente cadastrado, venda e conta a receber criadas. Documento ID: {resultado['documento_id']}.")`n                    elif analise["direcao_sugerida"] == "Nota Fiscal de Compra":`n                        st.success(f"Fornecedor cadastrado, compra e conta a pagar criadas. Documento ID: {resultado['documento_id']}.")`n                    elif analise["direcao_sugerida"] == "Nota de Empenho":`n                        st.success(f"Cliente identificado/cadastrado e processo documental criado. Documento ID: {resultado['documento_id']}.")`n                    else:`n                        st.info(f"Documento salvo para análise. Documento ID: {resultado['documento_id']}.")`n`n            except Exception as e:`n                st.error(f"Erro ao processar {arquivo.name}: {e}")`n`nelse:`n    st.info("Anexe um ou mais documentos para iniciar a classificação automática.")